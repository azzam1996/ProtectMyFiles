<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ProtectMyFiles" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen theme. -->

    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.ProtectMyFiles</item>
    </style>

</resources>